description = "Compress one Markdown file with HUI"
prompt = "Run /hui-compress {{args}}. Require an explicit file path. Before writing, state that compression replaces natural-language prose, preserves protected structure, and creates a backup. Refuse sensitive files. Use dry-run/preview when supported before changing a file."
