---
description: Write HUI project rule files
---

Run `node src/tools/hui-init.js $ARGUMENTS` when this local HUI checkout contains the script. Otherwise tell the user to run the HUI installer CLI with `--with-init`. Use `--dry-run` before changing existing rule files unless `--force` was explicitly requested.
